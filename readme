# ItsProyect

Este es el entorno de trabajo con la configuración de las siguientes herramientas:

1. Django
2. DRF (Django Rest Framework)
3. SimpleJWT DRF (Utilitario para autenticación)

## Instalación

### 1. Activación del entorno virtual

Para la instalación de requerimientos es necesario activar un entorno virtual:

```bash
python -m venv venv
```

```bash
source venv/bin/activate
```

### 2. Instalación de requerimientos

```bash
pip install -r requirements.txt
```

## Preparación del Backend

### 1. Migración de datos

Para migrar toda la configuración y alterar las tablas necesarias para el uso de SimpleJWT, ejecute el siguiente comando:

```bash
python manage.py migrate
```


